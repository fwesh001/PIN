/** @type {import('next').NextConfig} */
const nextConfig = {
  // Bind to all interfaces and the port pxxl expects (3001).
  // HOSTNAME/PORT are also exported via the start script for safety.
  reactStrictMode: true,
};

export default nextConfig;
